<%= form_for(@comment) do |f| %>
	<div class = "field">
		<p>Comment: </p>
		<%= f.text_field :content %>
	</div>
	<div class = "field">
		<p></p>
		<%= f.hidden_field :user_id, :value => current_user.id %>
	</div>
	<div class = "field">
		<p>Event: <%= f.collection_select :event_id, Event.all, :id, :title %></p>
	</div>
	<div class = "actions">
		<%= f.submit %>
	</div>
	<div class="error-msg">
		<% if @comment.errors.any? %>
		<ul>
			<% @comment.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
			<% end %>
		</ul>
		<% end %>
	</div>
<% end %>